# ── Required ───────────────────────────────────────────────────
MONGODB_URI=mongodb+srv://<user>:<password>@cluster.mongodb.net/?retryWrites=true&w=majority
MONGODB_DB_NAME=offline_pay
JWT_SECRET=your-super-secret-key-min-32-chars
PORT=4000

# ── Razorpay (get keys from dashboard.razorpay.com) ────────────
# Test mode keys start with rzp_test_
# Live mode keys start with rzp_live_
RAZORPAY_KEY_ID=rzp_test_XXXXXXXXXXXXXXXX
RAZORPAY_KEY_SECRET=XXXXXXXXXXXXXXXXXXXXXXXX

# Webhook secret — set in Razorpay Dashboard > Webhooks
RAZORPAY_WEBHOOK_SECRET=your_webhook_secret

# Razorpay X account number (for Payouts API — from RazorpayX dashboard)
RAZORPAY_ACCOUNT_NUMBER=XXXXXXXXXXXXXXXXXX

# ── Optional ───────────────────────────────────────────────────
JWT_EXPIRE=7d
NODE_ENV=development
HMAC_SECRET=your-hmac-secret-for-legacy-support

# LLM Provider (preferred): Hugging Face
HF_API_KEY=hf_xxxxxxxxxxxxxxxxxxxxxxxxxxxxx
HF_MODEL=meta-llama/Llama-3.1-8B-Instruct

# Optional fallback provider: Google Gemini
GEMINI_API_KEY=your_gemini_api_key
GEMINI_MODEL=gemini-2.0-flash
